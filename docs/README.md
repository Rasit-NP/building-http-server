# 작업 로그 인덱스

`building-http-server`의 일자별 작업 & 트러블슈팅 문서 목록.
새 문서 추가 시 **최신 항목이 위로** 오도록 한 줄씩 등록한다. (규칙: [`../CLAUDE.md`](../CLAUDE.md), 양식: [`_TEMPLATE.md`](_TEMPLATE.md))

- [2026-08-14] response-hardening — 08-11 남은 과제 3종(`ifstream` 실패 미검사로 인한 404 부재·`Content-Type` 부재·`StaticFileHandler` 테스트 부재)을 처리하고 `HttpResponse`에 `Date` 헤더 추가. 없는 파일·디렉터리(`fs::is_directory`)·traversal을 모두 `make_not_found()` 404로 통일(traversal의 08-11 임시 `400`을 404로 전환해 root 밖 파일의 존재 여부 자체를 은닉), 확장자를 `std::transform`+`std::tolower`로 정규화 후 `mime_from_extension()` 7종 매핑(미등록은 `application/octet-stream`), `serialize()`가 RFC 7231 IMF-fixdate `Date` 출력. `Date` 도입으로 `serialize()`가 wall clock에 의존하자 `serialize(std::time_t)` 오버로드를 본체로 두고 무인자 버전이 위임하는 구조로 분리해 전문 `EXPECT_EQ` 유지. 임시 디렉터리 fixture(비-템플릿 `StaticFileHandlerTester` + 파라미터 타입별 fixture 2종, `base_`/`root_` 2단 구조로 root 밖 secret까지 정리 범위 확보) 기반 테스트 14건 신설(`ctest` 34 → 48). 🔴 5건: (1) `std::transform` output iterator 누락으로 3-arg 호출 → 컴파일 실패, (2) `Date` 무조건 추가로 기존 `HttpResponseTest` 2건 파괴 — 전문 비교인데 `Date`는 호출마다 변해 하드코딩 불가(테스트가 깨진 게 아니라 비결정성이 드러남) → 시각 주입 오버로드로 해소, (3) `HttpResponse.h`가 `std::time_t`를 쓰며 `<ctime>` 미포함 → gcc/libstdc++만 `'std::time_t' has not been declared`(libc++는 transitive로 통과, `HttpResponse.cpp`는 `<ctime>`을 헤더보다 먼저 include해 우연히 성립, `StaticFileHandler.cpp`만 실패 / 07-12 `HttpRequest.h`+`<cctype>` 동종 재발), (4) `MIMETest`에 `INSTANTIATE_TEST_SUITE_P` 누락으로 8케이스 0건 실행(`UninstantiatedParameterizedTestSuite` 실패, 08-02 2-4 동종 재발), (5) `DirectoryTargetReturns404`가 선행 `/` 누락으로 `substr(1)` 후 `"ubdir"`가 되어 `is_open` 분기를 탐 — `is_directory` 미실행(그 검사가 실은 디렉터리 `ifstream` 읽기 예외를 막는 방벽임이 mutation으로 드러남). 🟡 9건: `mime_from_extension()` 정의만 하고 호출 누락 → `Content-Type` 여전히 부재(external linkage라 `-Wunused-function`이 침묵, 익명 namespace였다면 컴파일 시점 검출 — 08-02·08-10에 이은 세 번째 재발), `.jpg`/`.jpeg` 앞 `.` 누락으로 `extension()` 반환값과 절대 불일치, `<cctype>` 미포함, `http_date()` non-const 참조, 무인자 `serialize()` 미검증(자기 비교 assertion 추가했으나 초 경계 race 실측 5.9M회 중 2회≈3.4e-7 잔존), `TraversalReturns404`가 **네 라운드** 잔존 — 파일 미생성 → `ofstream` flush 미완(`status=200`으로 실제 유출됐는데 body가 비어 `EXPECT_NE` 통과) → 부정형 단언 → `create_directories`와 `ofstream`의 경로 기준 불일치로 조용한 쓰기 실패, lowercase 변환 미검증(케이스가 전부 소문자라 no-op), `ctest -j8` 임시 디렉터리 충돌(순차 14/14 vs `-j8` 3~6건 실패, `unique_name()`이 프로세스 내 카운터만 사용 — `base_` 정리 범위 확대가 피해를 0~4건에서 3~6건으로 증폭). Ubuntu 22.04 컨테이너 전체 빌드 성공·`ctest` **48/48 통과**(2.11s)·변경 TU 경고 0(`event_loop.cpp:113` 기존 건 제외), mutation testing **4/4 검출**(중간 단계에서는 `is_directory`·`inside`·MIME 세 검사를 통째로 제거해도 0/3 검출이었음), 기동 컨테이너 경로·MIME 매트릭스 8종 실측(`/`·`/index.html` → `200 text/html`, `/style.css` → `text/css`, `/pic.PNG` → `image/png`(대문자 정규화), 없는 파일·traversal → `404`), `Date: Fri, 14 Aug 2026 10:02:07 GMT` 확인. HEAD가 GET과 바이트 단위로 동일한 응답을 보내는 RFC 7230 §3.3 위반(raw socket 실측 body 151B)·`unique_name()` PID 미포함·헬퍼 external linkage·query string 미분리(`/index.html?v=1` → 404)·`Date` 중복 방지 부재·`gmtime_r` 반환값 미확인·`StaticFileHandler(const std::string root)`의 move 차단은 향후 과제 ([문서](2026-08-14-response-hardening.md))
- [2026-08-11] static-file-serving — 하드코딩 `"Success"` 응답을 걷어내고 `StaticFileHandler`(`root_` + `handle(const HttpRequest&) const`)를 도입해 `public/` 정적 파일 서빙으로 전환. `Connection`은 소켓 I/O·파싱만 담당하고 응답 내용은 `handler_.handle(req).serialize()`에 위임, handler는 `EventLoop`가 값으로 소유. `std::filesystem`으로 `base / target` 결합 후 `lexically_normal()`, 정규화된 full path의 base prefix + `/` 경계 검사로 traversal 차단(`/`→`/index.html` 매핑, `std::ios::binary` + `istreambuf_iterator` 읽기). 🔴 4건: (1) `src/CMakeLists.txt`에 `http/StaticFileHandler.cpp` 미등록 → `undefined reference to StaticFileHandler::handle` 링크 실패로 `ctest` 전체 차단(08-02 `HttpResponse.cpp` 동종 재발), (2) `accept_new()` 지역 변수 `StaticFileHandler handler("public")`를 `Connection`이 `const&`로 보유 → dangling reference, ASan `stack-buffer-overflow` `READ of size 6`(=`"public"`, 죽은 프레임을 `buf[4096]`이 재사용) → `EventLoop` 멤버로 승격, (3) traversal 가드가 세 결함 중첩으로 세 라운드 잔존 — `compare()`가 일치 시 `0`(=false)인데 그대로 `&&` 사용해 `inside` 항상 false + 변수명과 반대인 `if (inside)` + 본문이 빈 주석 → 1차 `/../CLAUDE.md`가 `200`(4203바이트 노출), 비교만 고친 2차엔 반대로 `/index.html`이 전부 `400`이고 traversal만 통과 → `compare(...) == 0` + `if (!inside)` + 실제 응답 반환으로 해소(404 전환은 후속), (5) `Dockerfile`이 `public/`을 `COPY`하지 않아 `/app`에 바이너리만 존재 → 브라우저 빈 화면(`200 / Content-Length: 0`), runtime 스테이지에 `COPY public/ ./public/` 추가. 🟡 1건: (4) 참조 멤버 추가로 move assignment가 암묵 삭제되는데 `= default` 유지 → clang `-Wdefaulted-function-deleted`(gcc는 침묵) → `= delete` 명시. Ubuntu 22.04 컨테이너 전체 빌드 성공·`ctest` **34/34 통과**(2.03s, `integration.idle_cpu` 포함)·신규 TU 경고 0(gcc·clang 양쪽, `event_loop.cpp:113` 기존 건 제외)·ASan/UBSan 리포트 0, `docker compose` 기동 후 경로 매트릭스 8종 실측(`/`·`/index.html`·`/./index.html` → `200 151B`, `/../CLAUDE.md`·`/../../etc/passwd`·`//etc/passwd` → `400` 차단), 1MB 바이너리 `cmp` IDENTICAL(`EPOLLOUT` backpressure 경로 실주행 확인). `ifstream` 실패 미검사로 없는 파일·빈 파일·디렉터리가 모두 `200 0B`(404 부재, 이번 (5)를 은폐한 장본인)·`Content-Type` 부재·`Connection: close` 비대칭·문서 root 하드코딩(`"public"` 상대 경로, CWD 암묵 계약)·method 미검사(405)·`StaticFileHandler` 테스트 부재는 향후 과제 ([문서](2026-08-11-static-file-serving.md))
- [2026-08-10] request-body-parsing — `HttpRequestParser`에 `Content-Length` 기반 request body 파싱 추가. `State::Body`/`parseBody()`/`body_remaining_` 도입, `HttpRequest::body` 신설, `parse_content_length()`가 `std::from_chars` + `ptr != end`(뒷꼬리 `5, 5`) + `result_out_of_range`(20자리 overflow) + 부호 사전 배제로 값 검증, `take = std::min(available, body_remaining_)`로 초과분 clamp(없으면 `size_t` underflow → `Incomplete` 무한), `reset()`에 `body_remaining_ = 0` 추가. 리뷰 중 드러난 message framing 모호성(중복 `Content-Length`)도 함께 처리. 🔴 1건: (3) 중복 거부 로직이 정반대로 동작 — `find(name)`이 정규화 전 원문인데 저장 키는 `to_lower(name)`이라 대문자 헤더는 항상 miss → `Content-Length: 5`/`: 3`이 그대로 `Ok`(body=`hel`), 비교는 `trim_ows` 이전 값이라 OWS 있으면 동일 값도 `Error`, 거부 범위가 전 헤더라 `Cookie` 2개도 400 → `name`/`value`를 선언 시점에 정규화하고 거부를 `content-length`로 한정(세 결함이 서로를 가려 하나만 고치면 악화되는 관계였음). 🟡 3건(전부 mutation testing으로 발견): (1) 청크 1 고정 급여로 clamp 분기가 원리적으로 미실행(`body_remaining_ >= 1` ∧ `available <= 1`) — 계측상 `parseBody()` 17회 중 clamp 0회 → 일괄 급여 병행, (2) 일괄 급여 추가 후에도 초과분 케이스 부재로 여전히 미검증 → `helloEXTRA` 케이스 추가, (4) 중복 테스트가 혼합 표기(`Content-Length`+`content-length`)뿐이라 둘째 줄이 우연히 소문자 키와 일치해 버그 파서도 통과 → 동일 대문자 표기·반복 일반 헤더 케이스 추가. ⚪ 1건: (5) `reset()`의 `body_remaining_` 초기화는 `parseBody()` 도달 경로가 대입을 반드시 통과해 관측 불가(equivalent mutant, 6개 시나리오 `diff` 일치) → 테스트 추가 없이 방어적 유지. Ubuntu 22.04 컨테이너 전체 빌드 성공(`http_server` 생성)·`ctest` **34/34 통과**(2.03s, `integration.idle_cpu` 포함)·신규 경고 0(`event_loop.cpp:114` 기존 건 제외), macOS 단위 테스트 33/33·ASan/UBSan 리포트 0·중복 헤더 12개 조합 불일치 0, mutation 12건 중 11건 검출. `Transfer-Encoding` 미대응(`TE`+`CL` 시 chunk framing을 body로 오인, 잔여 10바이트)·body에도 걸리는 8KB 상한(413이 적절)·`parse_content_length` external linkage(08-02 `reason_phrase()` 동종 재발)는 향후 과제 ([문서](2026-08-10-request-body-parsing.md))
- [2026-08-02] httpresponse-serialization — `connection.cpp`의 익명 namespace `build_response()`(status line + `Content-Length` + `Connection: close` 손조립)를 값 타입 `HttpResponse`(`status_code`/`headers`/`body`) + `serialize()`로 분리(07-22 남은 과제 "`HttpResponse` 타입 도입" 처리). `serialize()`가 `status_code`→`reason_phrase()` 매핑, `Content-Length`를 `body.size()`로 자동 산출하고 `headers` 중복 지정은 skip, `out.reserve()`로 append 재할당 절감. `connection.cpp`는 `Ok`/`Error` 분기에서 `HttpResponse` 설정 후 `serialize()` 사용(400 body를 `Bad request`→`Bad Request`로 정정). 🟡 2건(리뷰 지적): (1) `reason_phrase()`가 익명 namespace 밖 external linkage → 내부 링키지로 한정, (2) `Content-Length`를 호출부가 수동 지정하던 footgun → `serialize()` 단일 책임으로 자동화 + dedup skip 가드. 🔴 2건(테스트): (3) `HttpResponseTest`의 전역 `cases`가 07-13 파서 테스트의 `cases`와 `duplicate symbol '_cases'` 링크 충돌(+`CaseNameGenerator` 조용한 ODR) → 고유 이름 `ResponseCases`/`ResponseNameGenerator`로 회피, (4) `INSTANTIATE_TEST_SUITE_P` 누락으로 `TEST_P` 0건 실행/`UninstantiatedParameterizedTestSuite` 실패 → 인스턴스화 추가. macOS 로컬 `HttpResponse.cpp` 단독 컴파일·`connection.cpp` 구문 검사(epoll 외 에러 0) 통과, 단위 테스트 23/23 통과(`ResponseTest_200`/`_400` 실주입 확인), `ctest`는 `integration.idle_cpu`만 epoll 부재로 timeout(무관). epoll 의존 TU 전체 링크·미지 status code(`""` reason)·`HttpResponse` 커버리지 보강은 향후 과제 ([문서](2026-08-02-httpresponse-serialization.md))
- [2026-07-22] parser-server-integration — 07-02부터 남아 있던 "서버 흐름 편입" 과제 처리. `Connection`의 echo 로직을 걷어내고 `HttpRequestParser parser_`를 보유, `on_readable()`이 `parse()` 결과에 따라 `build_response()`로 `HTTP/1.1 <status>` + `Content-Length` + `Connection: close` 응답을 생성. 🔴 6건: (1) offset 6종을 `HttpRequestParser`→`HttpRequest`로 옮기며 `State::Done`의 `string_view` 대입 3줄이 옛 이름 참조(`error: use of undeclared identifier 'method_off_'`), (2) write 경로(`flush_write`/`on_writable`/`want_write`/`is_writing`/`set_writing`) 전면 주석 처리로 `event_loop.cpp:75,107,108,117` 빌드 차단 — macOS는 `sys/epoll.h` 부재로 미노출, 06-15 backpressure는 HTTP 응답에도 필요하므로 시그니처 유지한 채 원복(`EventLoop` 무수정), (3) `!method_len_||…` → `isSuccessfullyParsed()` 추출 시 부정 누락으로 정상 요청을 `Error`·빈 토큰을 통과, (4) `write_buf = "Success"` 가 status line 없는 raw 바이트라 `curl`이 `Received HTTP/0.9 when not allowed`로 거부, (5) `flush_write()`가 완료·`EAGAIN`을 모두 `true`로 반환해 응답 후 연결 미종료(클라이언트 두번째 `recv` 타임아웃) — `close_after_write` 플래그 + `finish_io()` 분리로 해소, (6) `Ok` 이후 read 루프 지속 → `break`. 🟡 `integration.*`가 echo 계약 전제라 Linux 실측 `single_echo`/`concurrent_echo`/`signal_shutdown` 15s 행·`large_payload` exit=1(4MB > `kMaxBufferBytes` 8KB) — HTTP 재작성은 후속으로 미루고 `add_test` 4건 등록 보류, `integration.idle_cpu`만 유지(C++ 미수정). Ubuntu 22.04 컨테이너에서 CI 절차 재현 — 빌드 exit 0·`ctest` 22/22 통과(2.03s)·TU `-Wall -Wextra -Wpedantic` 경고 0(`event_loop.cpp:114` 기존 건 제외). `curl` 200/400 응답 및 `Closing connection` 확인, raw socket으로 콜론 없는 헤더·잘못된 version → 400 후 FIN 확인. 🟡 통합 테스트 HTTP 재작성·전 시나리오 `TIMEOUT`·keep-alive(`reset()` 미호출)는 향후 과제 ([문서](2026-07-22-parser-server-integration.md))
- [2026-07-21] string-view-request-line — `HttpRequest`의 request line 3필드(`method`/`path`/`version`)를 `std::string` 복사에서 `buffer_`를 가리키는 `std::string_view`(zero-copy)로 전환. `parseRequestLine()`의 `request_line = buffer_.substr(0, crlf)` 임시 복사를 제거하고 `buffer_` 위에서 직접 `find`, 위치를 `method_off_/len_`·`path_off_/len_`·`version_off_/len_` 멤버로 기록. dangling 회피 위해 `string_view` 대입은 `State::Done`으로 이동. 🔴 3건: (1) 대입 전 검증 — `isValid()`/`isValidVersion()`가 빈 view를 검사해 전 요청 `Error`(대입+검증을 `Done`으로 이동해 해소), (2) 여분 공백 검사 `buffer_.find(' ', sp2+1)`의 `crlf` 경계 누락으로 헤더 공백(`Host: a:8080`)을 request line 공백 3개로 오인(`sp3 != npos && sp3 < crlf`로 해소, 중간에 `||` 오타 경유), (3) 빈 토큰 판정이 `Done`으로 밀려 종료 CRLF 없는 불완전 요청이 `Error` 대신 `Incomplete`(length 기반 `!method_len_||!path_len_||!version_len_` 검사를 `parseRequestLine`에 두어 조기 판정). 실패 이력 8→3→3→1→0. macOS 로컬 파서 테스트 `tests` 21/21 통과·파서 TU `-Wall -Wextra -Wpedantic` 경고 0. `integration.*`는 `sys/epoll.h` 부재로 미실행 → CI 대기. 🟡 `string_view` 수명 계약(`buffer_` 재할당 시 dangling)·offset 멤버 지역화는 향후 과제 ([문서](2026-07-21-string-view-request-line.md))
- [2026-07-13] test-parameterization — 개별 `TEST` 매크로로 흩어져 있던 `HttpRequestParserTest`를 GoogleTest value-parameterized test(`TEST_P`+`INSTANTIATE_TEST_SUITE_P`)로 재구성. 3개 fixture(정상 `ParserTest`+`HeaderCase` 3건 / 증분 `ChunkTest`+chunk 크기 `{1..5}` 스윕 / 에러 `ErrorTest`+`ErrorCase` 6건)와 헬퍼(`checkParsedHeader`·`Result` 반환형 `feedInChunks`), name generator 3종 도입 → 케이스 추가가 코드 블록이 아닌 테이블 한 줄로 축소. 파서 동작 변경 없음. 🔴 2건(빌드 차단: (1) 파일 끝 빈 이름 `TEST(HttpRequestParserTest, )` 스텁 잔존, (2) `gtest_discover_tests`가 빌드 단계 실행 중 `IsValidParamName` abort — `ErrorNameGenerator`의 콜론·공백 param name 위반, 접두사 `"ErrorTest: "`→`"ErrorTest_"` + `error_name` 값 공백 제거로 해소). macOS 로컬(Apple clang 21) 파서 관련 `ctest` 15/15 통과(0.06s)·테스트 TU `-Wall -Wextra` 경고 0·`--gtest_list_tests` 가독형 이름 등록 확인. `integration.*`는 `sys/epoll.h` 부재로 미실행 → CI 대기. 🟡 `feedInChunks` 미초기화 `Result` 반환(빈 input 시 UB)·미사용 fixture 멤버 `parser`는 향후 과제 ([문서](2026-07-13-test-parameterization.md))
- [2026-07-12] parser-hardening — 관대하게 통과시키던 `HttpRequestParser`에 4종 거부 규칙 도입. (1) `HttpRequest::isValidVersion()` 추가로 version을 `HTTP/D.D`(정확히 8자)로 엄격 검증(07-02 `"FOO"` false-accept 과제 해소), (2) `parseRequestLine()` `crlf==npos` 분기에서 `\r` 선행 없는 bare LF(`\n`)를 `Error`로 거부(무한 `Incomplete` 대기 차단), (3) `parseHeaders()`가 colon 없는 줄을 `continue` 스킵 대신 `Error`, (4) `parse()` 진입부 `state_==Error` early-return latch(기존 switch `State::Error` case와 기능상 중복이나 계약 명시)·`ErrorLatch` 테스트로 고정. 신규 테스트 4종(`RequestLine1/2/3`+`ErrorLatch`). macOS 로컬(Apple clang 21) 파서 `ctest` 10/10 통과·파서 `-Wall -Wextra` 경고 0. `integration.*`는 `sys/epoll.h` 부재로 미실행 → CI 대기. 🟡 `HttpRequest.h`가 `std::isdigit`용 `<cctype>`를 직접 include하지 않고 `HttpUtil.h` 포함 순서에 의존(단독 포함 시 취약)·method/path 형식 미검증은 향후 과제 ([문서](2026-07-12-parser-hardening.md))
- [2026-07-06] incremental-parsing — 호출마다 상태를 잃던 `HttpRequestParser`를 내부 `buffer_` 누적 + `offset_` 진행 위치 유지의 stateful 증분 파서로 리팩토링(07-05 "`Incomplete` 실동작" 과제 해소). `parse()`는 `buffer_.append` 후 `while(true)` 상태 루프로 dispatch, `find_crlf`는 `const std::string&` 기반, `kMaxBufferBytes=8KB` 상한 도입. 토큰/CRLF 중간을 쪼갠 5조각을 급식하는 `incremental` 테스트 추가(6종). 🔴 1건(`parse()`가 하위 파서 성공 후 `state_=Headers` 재대입 → `Done` 소실로 완결 요청도 `Incomplete`, `normal`/`extract`/`incremental` 3종 실패 → 성공 후 재대입 제거·`Done`에서 `Ok` 종결) + 🟡 2건(`find_crlf` 빈 버퍼 `size()-1` 언더플로 → `size()<2` 가드, 8KB 초과 early return에 `state_=Error` 미설정) + ⚪ 死코드 `kNpos` 제거. macOS 로컬(Apple clang 21) `ctest` 파서 6/6 통과·`-Wall -Wextra` 경고 0. `integration.*`는 `sys/epoll.h` 부재로 미실행 → Ubuntu/CI 검증 대기. 서버 흐름 편입·중복 헤더 정책은 향후 과제 ([문서](2026-07-06-incremental-parsing.md))
- [2026-07-05] http-header-parsing — request line만 파싱하던 `HttpRequestParser`를 `State::Headers`까지 확장. `HttpRequest`에 `unordered_map<string,string> headers` 추가, 신규 `HttpUtil.h`(`to_lower`/`trim_ows`)와 파일 스코프 `find_crlf` 도입, `parseHeaders`가 `:` 기준 name/value 분리 후 name lowercase·value OWS trim, 빈 줄이면 `Done`/`Ok`·라인 끊기면 `Incomplete`. `Ok` 시맨틱이 "헤더 blank line까지 완결"로 확장. 빌드 차단 4건(🔴: `find_crlf` 인자 `size`→`len`, `parseHeaders` 종료 `return Incomplete` 누락, request line 후 `offset=crlf+2` 미전진→request line 재파싱, 테스트 미한정 `Result::Ok`) + 🟡 2건(`extract` `assert`→`EXPECT_TRUE`, `normal` 회귀→입력에 `\r\n\r\n` 추가) 수정. macOS 로컬(Apple clang 21) `ctest` 파서 5/5 통과·신규 코드 경고 0, 독립 드라이버 18/18(colon-in-path 회귀 포함). `integration.*`는 `sys/epoll.h` 부재로 macOS 미실행 → Ubuntu/CI 검증 대기. `Incomplete` 실동작·중복 헤더 정책·서버 흐름 편입은 향후 과제 ([문서](2026-07-05-http-header-parsing.md))
- [2026-07-02] http-request-parser — echo 서버 위 첫 HTTP 계층으로 `HttpRequest`(`method`/`path`/`version` + `isValid()`)와 `HttpRequestParser`(request line만) 도입, `Result{Ok,Incomplete,Error}`·`State{RequestLine,Headers,Done,Error}` 골격 선언. 파서는 `\r\n` 분리 후 SP 2개 기준 정확히 3토큰 분해(3번째 공백·토큰 부족·빈 토큰 거부), 단위 테스트 4종 추가. 🟡 partial-read를 `Error`로 고정하던 `noCRLF` 테스트 제거(`Incomplete` 재설계를 서버 통합 시점으로 연기). Ubuntu 22.04 빌드 성공(신규 http 파일 경고 0)·`ctest` 15/15 통과(2.03s). version 형식 미검증(`"FOO"` false-accept)·`Incomplete` 실동작·`State::Headers` 파싱은 향후 과제 ([문서](2026-07-02-http-request-parser.md))
- [2026-06-30] integration-tests — 서버를 `fork`+`execv`로 띄워 검증하는 블랙박스 통합 테스트 5종(`single_echo`/`concurrent_echo`/`large_payload`/`idle_cpu`/`signal_shutdown`) 도입, `ctest` 등록. 하니스 계약을 위해 서버에 포트 인자 + `PORT <n>` 출력(`fflush`) + `getsockname` 기반 `local_port()` 추가. 빌드 차단 1건(🔴: `getsockname(fd, ...)`로 멤버 변수 `fd_` 대신 접근자 `fd()` 전달 → "invalid use of non-static member function") 수정. Ubuntu 22.04 빌드 성공·`ctest` 11/11 통과(2.03s). echo 시나리오 `TIMEOUT`·`idle_cpu` `SKIP_RETURN_CODE 77` 미설정은 향후 과제 ([문서](2026-06-30-integration-tests.md))
- [2026-06-27] self-pipe-wakeup — 시그널 핸들러에서 self-pipe write로 `epoll_wait(-1)`를 즉시 깨우고 check-then-block race를 차단. 빌드/실행 차단 1건(🔴: `register_wake_fd`에 read end 대신 `epoll_fd` 전달 → `epoll_ctl` 자기참조 `EINVAL`로 self-pipe 무력화) 수정. 빌드 성공(에러 0)·런타임 `SIGTERM` graceful shutdown(0.12s, exit 0) 검증 통과. `epoll_wait`는 `SA_RESTART` 무관 `EINTR` 반환이라 적용 전/후 종료 지연 대조는 미재현(🟡 검증 설계) ([문서](2026-06-27-self-pipe-wakeup.md))
- [2026-06-15] write-backpressure — non-blocking 부분 쓰기 대응으로 `flush_write()` 추출 + `on_writable()`/`EPOLLOUT` 토글(`update_interest`) 도입. 빌드 차단 2건(🔴: write 관심 등록에 `EPOLLOUT` 대신 `EPOLLET` 사용, 등록된 fd에 `EPOLL_CTL_ADD` 재호출→`EEXIST`) 수정. 빌드 성공(🟡 `-Wextra` enum/int 경고 1건)·런타임 echo(small/4MB/동시 5연결) 검증 통과. half-close 시 미전송 데이터 유실은 향후 과제 ([문서](2026-06-15-write-backpressure.md))
- [2026-06-14] eventloop-connection-refactor — 인라인 epoll 루프를 `EventLoop` 클래스로, client 상태를 `Connection`(+`unique_ptr` 소유)으로 분리. 빌드 차단 3건(🔴: move-only `Socket`을 값 매개변수에 lvalue로 전달→복사 삭제, `std::move` 후 `socket.fd()`로 epoll 등록→`EBADF`, `event_loop.h` `::close`에 `<unistd.h>` 누락) 수정. 빌드·`ctest` 6/6·런타임 echo 검증 통과 ([문서](2026-06-14-eventloop-connection-refactor.md))
- [2026-06-09] epoll-multiplexing — `epoll`(level-triggered) 이벤트 루프로 다중 연결 echo 전환, accept한 client `Socket`을 `std::unordered_map<int, Socket>`로 소유. 빌드 차단 3건(🔴: `MAX_EVENTS` 미선언, accept `Socket` 즉시 소멸→연결 끊김, `clients` 맵을 `for` 루프 안 선언→`at()` throw/`std::terminate`) 수정. 빌드·`ctest` 6/6·런타임 echo 검증 통과 ([문서](2026-06-09-epoll-multiplexing.md))
- [2026-06-08] nonblocking-io — listening/client socket을 `O_NONBLOCK`으로 전환, `handle_client()` 분리, 종료 플래그를 `std::atomic<bool>`(lock-free)로 교체. 빌드 차단(🔴: `accept()` optional 반환값 폐기 + `Socket`/`nullopt` 비교) 수정 ([문서](2026-06-08-nonblocking-io.md))
- [2026-06-07] socket-unit-tests — 더미 `calculator` 모듈 제거, 테스트를 RAII `Socket` 단위 테스트 6종으로 전환. 빌드 차단 2건(🔴: `sockets` 미링크 → undefined symbols, `sockets` include 경로 누락 → `socket.h` not found) 발견·수정 ([문서](2026-06-07-socket-unit-tests.md))
- [2026-06-05] graceful-shutdown — `SIGINT`/`SIGTERM` 핸들러 + `accept()`→`std::optional<Socket>`로 graceful shutdown 구현. idle 종료는 정상, 활성 연결 중 종료는 미보장(향후 과제) ([문서](2026-06-05-graceful-shutdown.md))
- [2026-06-02] socket-refactoring — `main.cpp` echo 로직을 RAII `Socket` 클래스로 분리, 빌드 통과 ([문서](2026-06-02-socket-refactoring.md))
